- page_title("Camp vehicles")

.row
  .col-md-12
    .card.mb-3
      = card_header("truck", "Camp vehicles", pagy: @pagy, buttons: [header_admin_new_button(:camp_vehicle)])
      .card-table
        table.table
          thead
            tr
              th ID
              th User
              th Vehicle type
              th
          tbody
            - @resources.each do |resource|
              tr
                td = admin_link_to_id(resource)
                td = admin_link_to_user(resource.user)
                td = camp_vehicle_badge(resource)
                td.text-end = admin_edit_icon(resource)
      = pagy_card_footer(@pagy)

