- unless current_user.camp_interview&.finished?
  - if current_user.camp_interview.present?
    .alert.alert-secondary You must complete your #{(link_to_camp(current_user.camp_interview, "interview"))}.
  - else
    .alert.alert-secondary You must complete an #{link_to_guide("interview", :interview)}.