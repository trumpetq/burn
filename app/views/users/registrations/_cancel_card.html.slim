.card.mb-3
  .card-header Cancel
  .card-body
    p.card-text This <b>can not</b> be undone!
    = link_to("Close account", registration_path(resource_name), class: "btn btn-danger", data: {confirm: "Are you sure?", turbo_method: :delete})
